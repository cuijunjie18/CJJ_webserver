#include <bits/stdc++.h>
using namespace std;

template<typename T>
void compare(T a,T b){
    if (a > b){
        std::cout << a << ">" << b << "\n";
    }else{
        std::cout << a << "<" << b << "\n";
    }
}

template<typename T>
void add(T a,T b){
    std::cout << a + b << "\n";
}

template<typename T>
queue<T> que;

int main()
{
    // add(4,5);
    // add(3.7,6.8);
    int x = 10;
    double y = 0.1;
    string k = "12313";
    que<int>.push(x);
    que<double>.push(y);
    que<string>.push(k);
    while (!que<int>.empty())
    return 0;
}